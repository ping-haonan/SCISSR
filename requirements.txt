# SCISSR dependencies (tested on Python 3.10, CUDA 12.x).
#
# IMPORTANT: install a torch/torchvision build that matches your CUDA version
# first (https://pytorch.org/get-started/locally/), then `pip install -r requirements.txt`.

# --- Deep learning framework (versions we used for the paper) ---
torch==2.9.0
torchvision==0.24.0

# --- SAM 2 backbone build / config ---
hydra-core==1.3.2
omegaconf==2.3.0
iopath==0.1.10

# --- Numerical / imaging ---
numpy==2.2.2
opencv-python==4.11.0.86
pillow==11.1.0
scipy==1.15.1
scikit-image==0.25.1
scikit-learn==1.6.1
pandas==2.2.3
einops==0.8.0
PyYAML==6.0.2

# --- Augmentation / scribble synthesis ---
albumentations==2.0.3
kornia==0.8.0
voxynth==0.0.0

# --- Utilities / visualization / logging ---
tqdm==4.67.1
matplotlib==3.10.0
wandb==0.23.0

# --- Optional: only needed for some debug visualizations (lazy-imported) ---
# neurite==0.2
